version: '3.8'

services:
  return-bot:
    build: .
    container_name: return-bot
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "8000:8000"  # Web API
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
