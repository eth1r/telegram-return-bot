# ЧЕКЛИСТ ДЕПЛОЯ

## ПЕРЕД ДЕПЛОЕМ

- [ ] Docker и Docker Compose установлены на сервере
- [ ] Получен Telegram Bot Token от @BotFather
- [ ] Получен OpenAI API Key
- [ ] Создана группа операторов в Telegram
- [ ] Бот добавлен в группу операторов
- [ ] Получен ID чата операторов (через getUpdates API)
- [ ] Проект склонирован на сервер

## НАСТРОЙКА

- [ ] Скопирован `.env.example` в `.env`
- [ ] Заполнен `TELEGRAM_BOT_TOKEN`
- [ ] Заполнен `OPERATOR_CHAT_ID`
- [ ] Заполнен `OPENAI_API_KEY`
- [ ] Проверена модель OpenAI (по умолчанию `gpt-4o-mini`)
- [ ] Установлен уровень логирования (по умолчанию `INFO`)

## ДЕПЛОЙ

- [ ] Выполнен `chmod +x deploy.sh`
- [ ] Запущен `./deploy.sh`
- [ ] Контейнер запущен (проверка: `docker-compose ps`)
- [ ] Логи не содержат ошибок (проверка: `docker-compose logs`)

## ТЕСТИРОВАНИЕ

- [ ] Бот отвечает на `/start` в Telegram
- [ ] Бот задаёт вопрос "Как вас зовут?"
- [ ] Бот собирает все поля заявки
- [ ] Бот отправляет заявку в чат операторов
- [ ] Формат заявки корректный
- [ ] Клиент получает подтверждение

## МОНИТОРИНГ

- [ ] Настроен мониторинг логов
- [ ] Проверен автозапуск контейнера (restart: unless-stopped)
- [ ] Документированы команды управления

## БЕЗОПАСНОСТЬ

- [ ] Файл `.env` не закоммичен в Git
- [ ] Права доступа к `.env` ограничены (chmod 600)
- [ ] SSH доступ к серверу защищён ключами
- [ ] Firewall настроен

## BACKUP

- [ ] Создан backup `.env`
- [ ] Документирован процесс восстановления

---

## БЫСТРЫЕ КОМАНДЫ

```bash
# Статус
docker-compose ps

# Логи
docker-compose logs -f

# Перезапуск
docker-compose restart

# Остановка
docker-compose down

# Обновление
git pull && docker-compose up -d --build
```
